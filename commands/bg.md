---
name: bg
description: "Runs any slash command in a background tmux window so you can keep working in your current window."
---

# /bg

This command is a thin entry point; the canonical process lives in the
`bg` skill — single source of truth (SPEC-023). The two used to be
maintained as parallel prose copies and drifted.

Read `.claude/skills/bg/SKILL.md` and execute its process with these arguments:

$ARGUMENTS
