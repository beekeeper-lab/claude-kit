---
name: commands
description: "Prints a quick-reference table of all available slash commands with usage and flags."
---

# /commands

This command is a thin entry point; the canonical process lives in the
`commands` skill — single source of truth (SPEC-023). The two used to be
maintained as parallel prose copies and drifted.

Read `.claude/skills/commands/SKILL.md` and execute its process with these arguments:

$ARGUMENTS
