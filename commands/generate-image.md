---
name: generate-image
description: "Generate image assets from text prompts using Gemini's Nano Banana models."
---

# /generate-image

This command is a thin entry point; the canonical process lives in the
`generate-image` skill — single source of truth (SPEC-023). The two used to be
maintained as parallel prose copies and drifted.

Read `.claude/skills/generate-image/SKILL.md` and execute its process with these arguments:

$ARGUMENTS
