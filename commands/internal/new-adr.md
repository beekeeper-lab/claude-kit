---
name: new-adr
description: "Document a significant technical or architectural decision with context, options, tradeoffs, rationale, and consequences. ADRs create a searchable decision history so the team understands *why* the codebase looks the way it does -- not just *what* it does."
---

# /new-adr

This command is a thin entry point; the canonical process lives in the
`new-adr` skill — single source of truth (SPEC-023). The two used to be
maintained as parallel prose copies and drifted.

Read `.claude/skills/internal/new-adr/SKILL.md` and execute its process with these arguments:

$ARGUMENTS
