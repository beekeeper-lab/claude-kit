---
name: scaffold-project
description: "Set up the complete repo skeleton for a Foundry-generated project. This creates the directory structure, CLAUDE.md, agent wrappers, context documents, and output folders that all personas and skills operate within. Run this before /internal:compile-team and /internal:seed-tasks."
---

# /scaffold-project

This command is a thin entry point; the canonical process lives in the
`scaffold-project` skill — single source of truth (SPEC-023). The two used to be
maintained as parallel prose copies and drifted.

Read `.claude/skills/internal/scaffold-project/SKILL.md` and execute its process with these arguments:

$ARGUMENTS
