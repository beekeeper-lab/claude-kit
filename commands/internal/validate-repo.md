---
name: validate-repo
description: "Verify that a project's structure, files, internal links, and stack-specific tooling are all in good shape. Catches missing files, broken references, secrets exposure, and configuration drift before they cause problems during development or export."
---

# /validate-repo

This command is a thin entry point; the canonical process lives in the
`validate-repo` skill — single source of truth (SPEC-023). The two used to be
maintained as parallel prose copies and drifted.

Read `.claude/skills/internal/validate-repo/SKILL.md` and execute its process with these arguments:

$ARGUMENTS
